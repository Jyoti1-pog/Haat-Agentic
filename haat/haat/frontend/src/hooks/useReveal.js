import { useRef } from 'react'

// Animations removed — this hook is a no-op kept for import compatibility.
// eslint-disable-next-line no-unused-vars
export function useReveal(_options) {
  return useRef(null)
}
