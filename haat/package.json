{
  "name": "haat",
  "version": "1.0.0",
  "description": "haat \u2014 digital goods from Indian craftspeople, with agentic checkout",
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"yellow,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix mcp-server",
    "build": "npm run build --prefix frontend",
    "verify": "npm run demo:agent --prefix backend",
    "mcp": "node mcp-server/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
